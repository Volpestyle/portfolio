# Example env vars for pnpm chat:preprocess
# Copy to scripts/chat-preprocess.env (gitignored) and fill in the secrets below.
GH_TOKEN=
PORTFOLIO_GIST_ID=
OPENAI_API_KEY=
# Optional: tweak concurrency when generating repo knowledge
PROJECT_KNOWLEDGE_CONCURRENCY=3
