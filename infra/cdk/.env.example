# Example configuration for the CDK deploys
# Copy to ../../.env.cdk or provide CDK_ENV_FILE when running `pnpm cdk ...`

# Optional custom domain wiring (Route53 hosted zone must already exist for automatic DNS validation)
# APP_DOMAIN_NAME=portfolio.example.com
# APP_HOSTED_ZONE_DOMAIN=example.com
# APP_ALTERNATE_DOMAINS=www.portfolio.example.com,api.portfolio.example.com
# APP_CERTIFICATE_ARN=arn:aws:acm:us-east-1:123456789012:certificate/abc-def

# Environment variables that should be injected into each Lambda
APP_ENV_VARS=NEXT_PUBLIC_SITE_URL
APP_ENV_PREFIXES=NEXT_,PORTFOLIO_
APP_ENV_BLOCKLIST=OPENAI_API_KEY,AWS_SECRET_ACCESS_KEY,AWS_ACCESS_KEY_ID,GH_TOKEN,UPSTASH_REDIS_REST_TOKEN
NEXT_PUBLIC_SITE_URL=https://portfolio.example.com
# Secrets such as OPENAI_API_KEY are resolved from AWS Secrets Manager at runtime.
